#include <iostream>

bool check(const char *word, const char *pattern) {
    std::cout << word << std::endl;
    return false;
}

void enumerate(const wchar_t *word, size_t length, const wchar_t *const pattern) {
    for(unsigned i = 0; i < length; ++i) {
        if
    }
}

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}